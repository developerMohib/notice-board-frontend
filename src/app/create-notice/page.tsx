import CreateNoticeForm from '@/components/forms/CreateNoticeForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <CreateNoticeForm />
        </div>
    );
};

export default page;