import Noticetable from "@/components/ui/Noticetable";
import Pagination from "@/components/ui/Pagination";

const page = () => {

    return (
        <div>
            <Noticetable />
            <Pagination />
        </div>
    );
};

export default page;